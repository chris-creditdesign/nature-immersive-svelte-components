import { Story, Canvas } from "@storybook/addon-docs/blocks";

# Header

Simple component to use, to set the header level of a headline - or to set it to be a `p` if the headerLevel is `none`.

<Canvas>
  <Story id="layout-components-header--default" />
</Canvas>
